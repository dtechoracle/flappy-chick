generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(cuid())
  username  String   @unique
  email     String?  @unique
  coins     Int      @default(0)
  inventory String   @default("{\"shield\":0,\"slowMo\":0,\"scorex2\":0,\"tinyBird\":0,\"widePipes\":0}")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  scores    Score[]
  
  @@index([username])
}

model Score {
  id        String   @id @default(cuid())
  score     Int
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
  
  @@index([userId])
  @@index([score])
  @@index([createdAt])
}
